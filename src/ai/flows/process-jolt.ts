'use server';
// Fluxo desativado. O sistema agora utiliza o motor Jolt Local (jolt-lite.ts)
// para garantir custo zero, privacidade e execução instantânea no browser.
export async function processJoltTransformation(input: any) {
  return null;
}
